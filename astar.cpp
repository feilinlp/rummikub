#include <bits/stdc++.h>
#include "astar.h"
#include "group.h"
#include "card.h"

using namespace std;

pair< vector<Group>, vector<Card> > aSolve(vector<Card> cards) {
    vector<Group> groups;

    return make_pair(groups, cards);
}
