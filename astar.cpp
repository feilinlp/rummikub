#include <bits/stdc++.h>
#include "astar.h"
#include "card.h"

using namespace std;

vector< pair<Card, int> > aSolve(vector<Card> cards) {
    vector< pair<Card, int> > groups;

    return groups;
}
